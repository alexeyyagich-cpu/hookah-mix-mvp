/** Grams threshold below which inventory items are flagged as low stock */
export const LOW_STOCK_THRESHOLD = 50

/** Default toast/notification display duration in ms */
export const TOAST_TIMEOUT = 3000

/** Stripe tip message max length */
export const STRIPE_MESSAGE_MAX_LENGTH = 1000
