# =============================================================================
# Supabase Configuration
# =============================================================================
# Get these from your Supabase project settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key (for server-side operations only, never expose to client)
# Required for Stripe webhook to update user subscription
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# Stripe Configuration
# =============================================================================
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Webhook secret (from Stripe Dashboard -> Developers -> Webhooks)
# Create webhook endpoint: https://your-domain.com/api/stripe/webhook
# Events to subscribe: checkout.session.completed, customer.subscription.created,
#   customer.subscription.updated, customer.subscription.deleted,
#   invoice.payment_succeeded, invoice.payment_failed
STRIPE_WEBHOOK_SECRET=whsec_...

# Price IDs (create products/prices in Stripe Dashboard)
# Pro Plan
NEXT_PUBLIC_STRIPE_PRICE_PRO_MONTHLY=price_...
NEXT_PUBLIC_STRIPE_PRICE_PRO_YEARLY=price_...

# Enterprise Plan
NEXT_PUBLIC_STRIPE_PRICE_ENTERPRISE_MONTHLY=price_...
NEXT_PUBLIC_STRIPE_PRICE_ENTERPRISE_YEARLY=price_...

# =============================================================================
# App Configuration
# =============================================================================
# Your app URL (for Stripe redirect URLs and password reset links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Demo mode — set to "true" to enable demo mode with fake data.
# When not set or set to anything else, demo mode is OFF.
# NEXT_PUBLIC_DEMO_MODE=true

# =============================================================================
# Email (Resend) — Optional
# =============================================================================
# Get from https://resend.com/api-keys
# Required for: low-stock email alerts, order status notifications
# RESEND_API_KEY=re_...
# RESEND_FROM_EMAIL=notifications@hookah-torus.com

# =============================================================================
# Telegram — Optional
# =============================================================================
# Create a bot via @BotFather on Telegram
# Required for: Telegram notifications integration
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
# TELEGRAM_WEBHOOK_SECRET=your-random-webhook-secret
